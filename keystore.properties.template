# Template for keystore properties — DO NOT COMMIT real credentials
# Copy to keystore.properties and fill values, or use GitHub Secrets

STORE_FILE=keystore.jks
STORE_PASSWORD=your_keystore_password
KEY_ALIAS=your_key_alias
KEY_PASSWORD=your_key_password
